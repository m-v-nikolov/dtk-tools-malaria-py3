#from distutils.core import setup
#import setuptools
from setuptools import setup, find_packages

setup(name='malaria',
      version='1.091',
      packages=find_packages(),
      install_requires=[]
      )
